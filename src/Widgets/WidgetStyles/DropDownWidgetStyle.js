import metrics from "../../Metrics";

export const styles = {
  container: {
    marginLeft: 8,
    marginRight: 8,
    borderWidth: 1,
    borderRadius: metrics.textInputBorderRadius,
    borderColor: "#B0B0B0"
  }
};
